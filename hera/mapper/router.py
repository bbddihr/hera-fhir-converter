"""Router — form_type을 적절한 mapper로 디스패치한다.

    lab / *_result        → mapper.lab        (collection)
    anesthesia / 서사형    → mapper.narrative  (document)

Phase 0: 스텁. Phase 4에서 분기 정식화.
"""
from __future__ import annotations

# TODO(Phase 4): route(form_type) -> mapper 함수
